---
scope: FactoryShield full-system architecture spine (brownfield backfill)
purpose: build-substrate
altitude: feature
updated: 2026-07-02T00:00
---

- (event) run started: brownfield input — existing shipped codebase (src/, frontend/) + prior architecture doc docs/FULL_ARCHITECTURE.md + finalized prd.md (this run's own PRD workspace) + _bmad-output/project-context.md
- (note) uv/python unavailable in this environment; memlog.py could not be executed — entries appended manually in the same append-only format
- (decision) working mode: Fast path, chosen by user — ratify the paradigm and conventions the existing code already shows rather than re-derive from scratch
- (decision) deliverable scope: spine only, no additional audience-facing document, per user choice
- (decision by user) [ADOPTED] paradigm = Clean Architecture (layered): Domain -> Application -> Infrastructure -> Api, one-directional dependency flow, already consistently followed across ~9 sprints of shipped code
- (decision) AD-1: dependency direction is fixed one-way (Domain has zero project references; Application depends only on Domain; Infrastructure implements Application-defined interfaces; Api composes Infrastructure+Application). Binds: all backend projects. Prevents: a new feature adding an EF Core or HTTP reference into Domain, or Application calling an Infrastructure concrete class directly instead of its interface.
- (decision) AD-2: [ADOPTED] every write is a MediatR Command+Handler, every read a Query+Handler, under Application/<Feature>/Commands|Queries. Binds: all Application-layer business logic. Prevents: business logic leaking into controllers or being called directly from Infrastructure.
- (decision) AD-3: [ADOPTED] Incident.Status is mutated only through the injected IIncidentStateMachine.TransitionAsync(), which enforces a legal-transition adjacency map. Binds: all incident lifecycle code (governance gates, resolver actions, approver actions). Prevents: two independently-built features each hand-rolling incompatible status transitions that bypass the legal adjacency map.
- (decision) AD-4: [ADOPTED] FluentValidation validators are registered explicitly in Program.cs (AddScoped<IValidator<TCommand>, TValidator>()), never via assembly scanning. Binds: every new Command. Prevents: a validator existing in code but silently never executing because it was never wired up.
- (decision) AD-5: [ADOPTED] every backend endpoint gated by an authorization Policy must have a matching frontend gate (route guard and/or conditional nav/UI); the backend policy is the actual security boundary, the frontend gate is UX-only. Binds: all role-gated features, frontend and backend both. Prevents: a role seeing a nav link or UI affordance that 403s because only one side of the gate was implemented (recurred 3x already: Analytics nav, dashboard pending-approvals call, All Incidents scoping).
- (decision) AD-6: [ADOPTED] real-time (SignalR) auth is carried via query-string JWT (?access_token=), never the Authorization header, because browser WebSocket handshakes cannot set custom headers. Binds: NotificationHub and any future SignalR hub. Prevents: a new real-time feature inventing a second, incompatible auth-transport convention.
- (decision) AD-7: [ADOPTED] approval_events and incident_state_log rows are immutable at the database level (REVOKE UPDATE, DELETE for the app role) — no soft-delete, no admin override, anywhere in the app layer. Binds: audit-trail and governance-approval data. Prevents: a future admin/compliance feature adding an edit-history or undo capability on audit data.
- (decision) AD-8: [ADOPTED] severity is stored as an integer (1-4), never as a string; all labels are presentation-layer projections computed at the edge. Binds: Incident.Severity and anywhere severity is displayed. Prevents: a second feature (e.g. reporting) introducing a parallel string-based severity representation that drifts from the canonical int.
- (decision) AD-9: [ADOPTED] frontend is Angular standalone components only (no NgModules), state via signals (signal/computed/effect), templates use @if/@for control flow exclusively. Binds: all frontend components. Prevents: a new component reintroducing NgModule-based or *ngIf/*ngFor patterns that fragment the codebase's mental model.
- (decision) AD-10: [ADOPTED] charting uses the apexcharts core library wrapped by a hand-rolled ChartComponent (afterNextRender + effect); ng-apexcharts must never be installed/used because its peer-dependency requires Angular >=20 and this project is pinned to 19.2. Binds: any feature adding a chart. Prevents: a build break from a future contributor reaching for the more obvious ng-apexcharts wrapper.
- (decision) AD-11: [ADOPTED] all recurring/time-based backend work (SLA checks, escalation, stale-draft monitoring, notification reminders) runs through Hangfire recurring jobs backed by PostgreSQL storage, never an ad-hoc in-process timer. Binds: any new time-triggered feature. Prevents: a feature adding its own timer/polling loop that doesn't survive a process restart the way Hangfire jobs do.
- (constraint) [ADOPTED] Department is stored as a plain string, not a FK to a departments table — MVP has no department-management UI, so a FK would require seeding data that doesn't exist yet. Logged as Deferred, not silently diverged.
- (version) Angular 19.2.0, TypeScript 5.7.2 (strict+strictTemplates), RxJS ~7.8, apexcharts 5.15.2, zone.js ~0.15 — verified from frontend/package.json and tsconfig.json in this run.
- (version) .NET 10 (net10.0), EF Core 9.x + Npgsql 9.x, Hangfire 1.8.20 + Hangfire.PostgreSql 1.20.11, Microsoft.AspNetCore.Authentication.JwtBearer 10.0.9, Swashbuckle 10.2.3 — verified from src/FactoryShield.Api/*.csproj and src/FactoryShield.Infrastructure/*.csproj in this run.
- (question) [ASSUMPTION] no dedicated test project exists anywhere in the repo (backend or frontend) — flagged as an open gap rather than an invariant to enforce, since introducing one is a future decision, not a documented existing convention.
- (decision) Capability -> Architecture map derived from this run's own finalized prd.md (FR-1..FR-40, 5 groups) since a PRD was available as a driving input for this spine.
- (event) spine distilled from memlog, status set to final, updated 2026-07-02
