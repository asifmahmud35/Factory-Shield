# FactoryShield — System Architecture Document (Deprecated)

**⚠️ Superseded by [`FULL_ARCHITECTURE.md`](FULL_ARCHITECTURE.md).**

This was the first-draft architecture doc (30 Jun 2026). `FULL_ARCHITECTURE.md` (1 Jul 2026) is the newer, more complete version — it covers the same conflict-resolution/data-model/RBAC/API-contract ground plus the full migration list, story sequence, and frontend component structure. The two had drifted apart and duplicated most of their content, so this file is kept only as a historical pointer rather than merged line-by-line.

**For current architecture, always read `FULL_ARCHITECTURE.md`.** For current build status, read `docs/SPRINT_PLANNING.md` and `docs/API_PLAN.md` — both are more up to date than either architecture doc's own status tables.
